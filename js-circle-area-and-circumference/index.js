let radius = 5
let circleArea = Math.PI * Math.pow(radius, 2)
let circumference = 2 * Math.PI * radius
let text = "For a circle with a radius of " + radius + " units: The area is approximately " + circleArea + " square units. The circumference is approximately " + circumference + " units."
console.log(text)